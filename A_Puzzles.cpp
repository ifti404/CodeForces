#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;

int main()
{
    int n, m, result = 100000000;
    cin >> n >> m;
    vector<int> s(m);
    for (int i = 0; i < m; i++)
    {
        cin >> s[i];
    }

    sort(s.begin(), s.end());

    result = s[+n - 1] - f[k]
}